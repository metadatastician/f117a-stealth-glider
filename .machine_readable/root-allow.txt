# Root allowlist for f117a-stealth-glider — RSR root-shape contract, retrofitted 2026-09-02.
#
# Lists every entry permitted at the repository root. The check is BIDIRECTIONAL
# (scripts/check-root-shape.sh, taken from rsr-template-repo@2283f12):
#   * anything at root that is not listed here is drift, and fails;
#   * anything listed here WITHOUT the '?' marker must exist, and its absence fails.
# Git-ignored entries (node_modules/, tmp/, _site/) are not root entries.
#
# This repository predates the template's 2026-08 un-hide of machine-readable/,
# so its metadata tree is the dotted `.machine_readable/` and its README and
# CHANGELOG are Markdown. They are recorded here as they ARE, not as the
# template's current shape says they should be: migrating them is a separate
# decision, and an allowlist must not paper over it.
#
# Format: one entry per line; '#' starts a comment; trailing '/' marks a directory;
# '?' prefix = optional (permitted, not required); no prefix = required.

# ─── Authority and documentation files ───────────────────────────────────────
README.md                  # Markdown; pre-dates the estate AsciiDoc rule (see header)
CHANGELOG.md               # as above
AUDIT.adoc
ARCHITECTURE.md
VERIFICATION.md            # the authoritative record of how each claim was established
KERNEL-DIVERGENCE.md       # the declared vendor-and-fork relationship to f19@9effa596
GOVERNANCE.md
MAINTAINERS.adoc
CODE_OF_CONDUCT.md
CONTRIBUTING.md
SECURITY.md
CITATION.cff
LICENSE                    # AGPL-3.0-or-later: this repo's licence, not the template's MPL-2.0 default
LICENSES/                  # REUSE licence texts

# ─── Build and tooling ───────────────────────────────────────────────────────
justfile                   # lowercase; verify / build / test / contracts / root-shape / ci
package.json               # zero-dependency Node manifest (scripts + licence field)
mise.toml
k9iser.toml
.editorconfig
.gitattributes
.gitignore
.git/                      # a file in a linked worktree, a directory otherwise; both permitted

# ─── The game ────────────────────────────────────────────────────────────────
f117a-stealth-glider.html            # committed single-file bundle; CI (C10) proves it byte-reproducible from src/
f117a-stealth-glider.launcher.a2ml   # launcher contract
f117a-stealth-glider-launcher.sh     # launcher
src/
design/                    # level-design search scripts (measure / period / solve / sweep / place)
spike/                     # v0 spike, digest-pinned provenance (byte-identical or it is not provenance)
scripts/                   # contractiles.mjs, metadata-citations.mjs + check-root-shape.sh

# ─── Metadata and forge ──────────────────────────────────────────────────────
.machine_readable/         # dotted, pre-2026-08 layout (see header)
.github/
.well-known/
